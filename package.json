{
  "name": "narrabuilder",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && netlify deploy --prod --dir=dist",
    "deploy:preview": "astro build && netlify deploy --dir=dist"
  },
  "dependencies": {
    "astro": "^6.0.5",
    "gsap": "^3.15.0",
    "jszip": "^3.10.1",
    "sass": "^1.102.0"
  }
}
